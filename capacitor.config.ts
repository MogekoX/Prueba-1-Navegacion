import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto_Evaluacion_1_Marco_Montero',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
